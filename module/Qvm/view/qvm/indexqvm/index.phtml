<?php
use Zend\Validator\File\Count;
use Zend\Form\Element;
use Zend\Form\Element\Select;
use Zend\Form\Form;
?>

<strong>Module:</strong>
Qvm &raquo;
<strong>Controller:</strong>
Indexqvm &raquo;
<strong>Action:</strong>
index

<h1>Mes prochains Evenements</h1>
<table class="table">
	<tr>
		<th>Nom</th>
		<th>Date</th>
		<th>Vote</th>
	</tr>
<?php
$vote = array (
		1 => "OUI",
		2 => "NON",
		3 => "P-E",
		4 => "En Attente" 
);
$listVote = new  Select ();
$listVote->setAttributes($vote);

$form = $this->form;
$form->prepare();
echo $this->form()->openTag($form);

foreach ( $upcomingParticipatings as $upcomingParticipating ) :
	?>
<tr>
		<td><?php echo $this->escapeHtml($upcomingParticipating->title);?></td>
		<td><?php echo $this->escapeHtml($upcomingParticipating->date);?></td>
		<td><?php echo ($listVote->getAttribute($this->escapeHtml($upcomingParticipating->vote)));?></td>
		<td><?php echo $this->formRow($form->get('vote'));?></td>
	</tr>
<?php 
endforeach; ?>
</table>

<h1>Mes Evenements en attente de vote</h1>
<table class="table">
	<tr>
		<th>Nom</th>
		<th>Date</th>
		<th>Vote</th>
	</tr>
<?php
$nbEvtEnAttente = count ( $pendingParticipatings );

foreach ( $pendingParticipatingsLimit as $pendingParticipating ) :
	?>
<tr>
		<td><?php echo $this->escapeHtml($pendingParticipating->title);?></td>
		<td><?php echo $this->escapeHtml($pendingParticipating->date);?></td>
		<td><?php echo ($listVote->getAttribute($this->escapeHtml($pendingParticipating->vote)));?></td>
		<td><?php echo $this->formRow($form->get('vote'));?></td>
	</tr>
<?php endforeach; 
echo $this->form()->closeTag();
?>
</table>
<?php
if ($nbEvtEnAttente > 5) {
	?>
<p>
	<a class="btn btn-success btn-small" href="#"><?php echo $this->translate("Plus d'&eacute;v&egrave;nements") ?> &raquo;</a>
</p>
<?php

}
?>

<br>
<h1>Mes Groupes</h1>
<table class="table">
	<tr>
		<th>Nom</th>
		<th>Privacite</th>
	</tr>
<?php
$nbGroupe = count ( $groups );
foreach ( $groupsLimit as $group ) :
	?>
<tr>
		<td><?php echo $this->escapeHtml($group->label);?></td>
		<td><?php echo $this->escapeHtml($group->is_private);?></td>
	</tr>
<?php endforeach; ?>
</table>
<?php
if ($nbGroupe > 5) {
	?>
<p>
	<a class="btn btn-success btn-small" href="#"><?php echo $this->translate("Plus de groupes") ?> &raquo;</a>
</p>
<?php
}
?>